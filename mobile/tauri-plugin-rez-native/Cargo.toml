[package]
name = "tauri-plugin-rez-native"
version = "0.1.0"
edition = "2021"
links = "tauri-plugin-rez-native"

[build-dependencies]
tauri-plugin = { version = "2", features = ["build"] }

[dependencies]
tauri = { version = "2", features = [] }
